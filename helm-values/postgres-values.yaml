# Настройки для bitnami/postgresql.
# Пароль postgres и имя базы задаются в scripts/20-infra-install.sh через --set.

primary:
  persistence:
    enabled: true
    size: 20Gi

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 1000m
      memory: 1Gi
